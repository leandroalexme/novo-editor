import React from 'react';
import { StoreType } from '../model/store';
export declare const HistoryButtons: (({ store }: {
    store: StoreType;
}) => React.JSX.Element) & {
    displayName: string;
};
